pipelineJob("adminui-be") {
	description()
	keepDependencies(false)
	parameters {
		stringParam("ENV_NAME", "staging", "")
		stringParam("BRANCH_NAME", "dev", "")
	}
	definition {
		cpsScm {
			scm {
				git {
					remote {
						github("https://kaspr001@bitbucket.org/riqio/riq-admin-backend.git", "https")
						credentials("bitbucket")
					}
					branch("*/master")
				}
			}
			scriptPath("deployment/jenkins/build")
		}
	}
	disabled(true)
}
